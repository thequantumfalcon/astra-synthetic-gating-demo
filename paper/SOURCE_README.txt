ASTRA synthetic gating demo — source package (v5)

Contents:
- LaTeX manuscript sources (paper.tex, sections/, references.bib)

The bibliography is regenerated by the build below (bibtex), so no paper.bbl is shipped.

Build (example):
- pdflatex paper.tex
- bibtex paper
- pdflatex paper.tex
- pdflatex paper.tex
