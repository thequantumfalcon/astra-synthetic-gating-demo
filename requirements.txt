# Project runtime dependencies for the ASTRA synthetic gating demo.
# Unpinned, for general use. To reproduce the committed artifacts in the exact
# environment they were generated in, install requirements-lock.txt instead.

numpy

# Optional (not required for core artifact reproduction):
# matplotlib
